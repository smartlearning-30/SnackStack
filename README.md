# SnackStack 🍔  
A smart recipe and contact platform with a full email system built using Node.js, Express, and Nodemailer.

## 📂 Project Structure
- **frontend/** → All website pages, scripts, and styles.
- **backend/** → Node.js + Express server for email handling and API routes.

## 🚀 Tech Stack
- Frontend: HTML, CSS, JavaScript, Bootstrap
- Backend: Node.js, Express, Nodemailer
- Email Service: Gmail SMTP

## 🧠 Setup
1. Clone this repo
2. Run `npm install` inside `/backend`
3. Create a `.env` file in `/backend`:
4. Start the server:
5. Open `frontend/index.html` in browser
